# ./grafana/provisioning/dashboards.yml

apiVersion: 1

providers:
  - name: 'default-provider'
    orgId: 1
    folder: 'Dashboards del TP'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards